import React from 'react';

const Title = () => {
  const northCoders = '< Northcoders News />';
  return (
    <header className="Title">
      <h1>{northCoders}</h1>
    </header>
  );
};

export default Title;
